country_converter==1.0.0
dash==2.15.0
dash_bootstrap_components==1.3.1
dash_daq==0.5.0
Flask_Caching==2.0.2
ipython==8.11.0
# 1.26.4 rather than 1.21.5: scikit-learn arrives with relbench and pulls
# numpy 2.x, which pandas 1.5.2's compiled extensions cannot load against
# ("ValueError: numpy.dtype size changed" at import pandas). 1.26.4 is the
# last 1.x, and is the version the pickle reproduction was verified with.
numpy==1.26.4
openpyxl==3.0.10
pandas==1.5.2
plotly==5.13.0
python-dotenv
gunicorn
elasticsearch>=7.17,<8
dash-loading-spinners
psycopg[binary]==3.2.3
pyarrow==17.0.0

